# Gemini configuration for Phase 6 generation
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# One educational link for refusal output (must remain allowlisted)
REFUSAL_EDUCATIONAL_URL=https://www.amfiindia.com/investor-corner/knowledge-center

# Optional: set to 0 to skip writing generation-last.json every turn (slightly lower latency).
# GEN_WRITE_LAST_JSON=1
